<p id="notice"><%= notice %></p>

<h1>Company Quotes</h1>

<table>
        <tr>
                <td>Name</td>
                <td>Company</td>
                <td>EMail</td>
                <td>Phone</td>
        </tr>
        <% @companies.each do |company| %>
                <tr>
                        <td><%= company.name %></td>
                        <td><%= company.companyname %></td>
                        <td><%= company.email %></td>
                        <td><%= company.phone %></td>
                        <td><%= link_to "Show quote requests", company, method: :showQuote %></td>
                        <td><%= link_to "Edit", edit_company_path(company) %></td>
                        <td><%= link_to "Delete", company, method: :delete, data: { confirm: 'Are you sure?' } %></td>
                </tr>
                <tr>
                        <td>Ad Type</td>
                        <td>Number of Click Throughs</td>
                        <td>Target Demographics</td>
                        <td>Subtotal</td>
                </tr>
                <% @quotes.each do |quote| %>
                        <% if quote.companyID == company.id %>
                        <tr>
                                <td><%= quote.adtype %></td>
                                <td><%= quote.views %></td>
                                <td><%= quote.demographics %></td>
                                <td><%= quote.subtotal %></td>
                                <td><%= link_to 'Remove', quote, method: :delete %></td>
                        </tr>
                        <% end %>
                <% end %>
        <% end %>
    <tr>
      <td></td>
      <td></td>
      <td>Setup/Contract Fee:</td>
      <td>250</td>
    </tr>
</table>


<%= link_to "Employee Email Notification Manager", employee_email_managers_path %>
